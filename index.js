/**
 * @format
 */

import {AppRegistry} from 'react-native';
import GameManager from './src/components/GameManager';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => GameManager);
